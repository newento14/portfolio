'use client'
import {motion} from "framer-motion";

interface CardProps {
  delay: number,
}

const ProjectCard = ({delay}:CardProps) => {
  return (
    <motion.div>

    </motion.div>
  );
};

export default ProjectCard;